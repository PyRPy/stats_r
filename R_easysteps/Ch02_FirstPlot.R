# first plot
x <- c( 1, 3, 5, 7, 9 )
y <- c( 8, 0, 4, 6, 2 )
plot( x, y, type="o" )

plot(x, y, type = "b") # both in lines and points

plot(x, y, type = "l") # plot with lines

plot(x, y, type = "s") # plot with step like

plot(x, y, type = "h") # plot with histogram
